class LevelData():
    def __init__(self, map_path, waves, money_start, lives):
        self.map_path = map_path
        self.waves = waves
        self.money_start = money_start
        self.lives = lives  
        
